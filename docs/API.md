# API notes

The OpenAPI schema is generated by FastAPI at `/docs` and `/openapi.json`.

Correlation: send `x-request-id` / `x-correlation-id`. They are echoed on the response and bound into structured logs.

SSE events from `/research/{id}/events` are JSON objects with `message`, `research_id` and `ts`.
